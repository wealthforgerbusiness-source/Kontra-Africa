diff --git a/css/app.css b/css/app.css
index 54b8c9b..cb1f49e 100644
--- a/css/app.css
+++ b/css/app.css
@@ -694,6 +694,11 @@
   font-weight: 700;
   color: var(--color-primary);
   margin-top: var(--space-6);
+  background: var(--color-primary-light);
+  display: inline-flex;
+  align-items: baseline;
+  padding: var(--space-2) var(--space-5);
+  border-radius: var(--radius-full);
 }
 
 .paywall__currency {
@@ -707,6 +712,76 @@
   color: var(--color-text-muted);
 }
 
+.paywall__phone {
+  margin-top: var(--space-6);
+  width: 100%;
+  text-align: left;
+}
+
+.paywall__phone-label {
+  display: block;
+  font-size: var(--fs-sm);
+  font-weight: 600;
+  color: var(--color-text-muted);
+  margin-bottom: var(--space-2);
+}
+
+.paywall__phone-row {
+  display: flex;
+  align-items: stretch;
+  border: 1.5px solid var(--color-border-strong);
+  border-radius: var(--radius-md);
+  background: var(--color-surface);
+  transition: border-color var(--duration-fast) var(--ease-standard),
+              box-shadow var(--duration-fast) var(--ease-standard);
+}
+
+.paywall__phone-row:focus-within {
+  border-color: var(--color-primary);
+  box-shadow: 0 0 0 3px var(--color-primary-light);
+}
+
+.paywall__phone-divider {
+  width: 1px;
+  align-self: stretch;
+  margin: var(--space-2) 0;
+  background: var(--color-border);
+}
+
+.paywall__phone-select,
+.paywall__phone-input {
+  min-height: 46px;
+  border: none;
+  background: transparent;
+  padding: 0 var(--space-3);
+  font-size: var(--fs-base);
+  color: var(--color-text);
+}
+
+.paywall__phone-select {
+  flex: 0 0 auto;
+  max-width: 45%;
+  border-radius: var(--radius-md) 0 0 var(--radius-md);
+}
+
+.paywall__phone-input {
+  flex: 1 1 auto;
+  min-width: 0;
+  border-radius: 0 var(--radius-md) var(--radius-md) 0;
+}
+
+.paywall__phone-select:focus-visible,
+.paywall__phone-input:focus-visible {
+  outline: 3px solid var(--color-accent);
+  outline-offset: 1px;
+}
+
+.paywall__phone-error {
+  color: var(--color-error);
+  font-size: var(--fs-xs);
+  margin-top: var(--space-2);
+}
+
 .paywall__zone {
   margin-top: var(--space-6);
   min-height: 56px;
@@ -717,6 +792,19 @@
 
 .paywall__cta {
   width: 100%;
+  background: linear-gradient(135deg, var(--color-accent), var(--color-accent-dark));
+  letter-spacing: 0.01em;
+}
+
+.paywall__cta:hover {
+  transform: translateY(-1px);
+  box-shadow: 0 10px 28px rgba(255, 138, 61, 0.5);
+}
+
+.paywall__trust {
+  margin-top: var(--space-3);
+  font-size: var(--fs-xs);
+  color: var(--color-text-faint);
 }
 
 .paywall__logout {
diff --git a/css/profil.css b/css/profil.css
index c1e03bd..7ab4f41 100644
--- a/css/profil.css
+++ b/css/profil.css
@@ -136,6 +136,62 @@
   outline-offset: 2px;
 }
 
+/* Réabonnement — sélecteur pays + numéro Mobile Money */
+.resubscribe-phone-row {
+  width: 100%;
+}
+
+.resubscribe-phone-label {
+  display: block;
+  font-size: 0.8125rem;
+  font-weight: 600;
+  color: var(--color-text-muted, #6B6480);
+  margin-bottom: 6px;
+}
+
+.resubscribe-phone-inputs {
+  display: flex;
+  align-items: stretch;
+  border: 1.5px solid var(--color-border-strong, #D6D2E4);
+  border-radius: var(--radius-md, 10px);
+  background: #fff;
+  transition: border-color 0.15s ease, box-shadow 0.15s ease;
+}
+
+.resubscribe-phone-inputs:focus-within {
+  border-color: var(--color-primary, #5B3FE0);
+  box-shadow: 0 0 0 3px var(--color-primary-light, #EFEBFC);
+}
+
+.resubscribe-phone-select,
+.resubscribe-phone-input {
+  min-height: 46px;
+  border: none;
+  background: transparent;
+  padding: 0 12px;
+  font-size: 0.9375rem;
+  color: var(--color-text, #2A2140);
+}
+
+.resubscribe-phone-select {
+  flex: 0 0 auto;
+  max-width: 45%;
+  border-right: 1px solid var(--color-border, #E9E7F2);
+  border-radius: var(--radius-md, 10px) 0 0 var(--radius-md, 10px);
+}
+
+.resubscribe-phone-input {
+  flex: 1 1 auto;
+  min-width: 0;
+  border-radius: 0 var(--radius-md, 10px) var(--radius-md, 10px) 0;
+}
+
+.resubscribe-phone-select:focus-visible,
+.resubscribe-phone-input:focus-visible {
+  outline: 3px solid var(--color-accent, #FF8A3D);
+  outline-offset: 1px;
+}
+
 /* Boutons */
 .btn {
   min-height: 44px;
@@ -162,8 +218,15 @@
 }
 
 .btn-primary {
-  background: var(--color-primary, #5B3FE0);
+  background: linear-gradient(135deg, var(--color-accent, #FF8A3D), var(--color-accent-dark, #E3712A));
   color: #fff;
+  box-shadow: 0 6px 18px rgba(255, 138, 61, 0.35);
+  transition: transform 0.15s ease, box-shadow 0.15s ease;
+}
+
+.btn-primary:hover {
+  transform: translateY(-1px);
+  box-shadow: 0 8px 22px rgba(255, 138, 61, 0.45);
 }
 
 .btn-logout {
diff --git a/dashboard.html b/dashboard.html
index 8847fb9..44a7231 100644
--- a/dashboard.html
+++ b/dashboard.html
@@ -18,6 +18,8 @@
 
   <link rel="stylesheet" href="/css/tokens.css">
   <link rel="stylesheet" href="/css/app.css">
+</head>
+<body>
 
   <!-- Écran de chargement affiché pendant la vérification auth + abonnement -->
   <div class="app-loading" id="appLoading">
diff --git a/functions/src/webhook.js b/functions/src/webhook.js
index 77931c4..a865d6b 100644
--- a/functions/src/webhook.js
+++ b/functions/src/webhook.js
@@ -18,17 +18,41 @@ exports.chariowWebhook = async (req, res) => {
       data.customer?.custom_metadata ||
       {};
 
-    const firebaseUid = customMetadata.firebase_uid;
+    let firebaseUid = customMetadata.firebase_uid;
+    let userRef = null;
 
-    if (!firebaseUid) {
-      console.warn("Aucun firebase_uid trouvé dans le webhook.");
-      return res.status(200).json({ received: true, status: "skipped_no_uid" });
+    if (firebaseUid) {
+      userRef = db.collection("users").doc(firebaseUid);
+    } else {
+      // Filet de sécurité : certains événements Chariow (ex. paiements initiés
+      // hors de notre bouton "S'abonner") n'ont pas de custom_metadata attaché.
+      // On tente de retrouver le compte par email.
+      const buyerEmail =
+        data.customer?.email ||
+        data.sale?.customer?.email ||
+        data.buyer?.email ||
+        data.email ||
+        null;
+
+      if (buyerEmail) {
+        console.warn(`Aucun firebase_uid dans le webhook, tentative de résolution par email : ${buyerEmail}`);
+        const matches = await db.collection("users").where("email", "==", buyerEmail).limit(1).get();
+        if (!matches.empty) {
+          userRef = matches.docs[0].ref;
+          firebaseUid = matches.docs[0].id;
+        }
+      }
+
+      if (!userRef) {
+        console.warn("Aucun utilisateur trouvé (ni firebase_uid, ni email correspondant).");
+        return res.status(200).json({ received: true, status: "skipped_no_match" });
+      }
     }
 
-    const userRef = db.collection("users").doc(firebaseUid);
     const normalizedEvent = eventType.toLowerCase();
 
     if (
+      normalizedEvent.includes("successful.sale") ||
       normalizedEvent.includes("sale.completed") ||
       normalizedEvent.includes("order.completed") ||
       normalizedEvent.includes("license.created") ||
@@ -68,11 +92,24 @@ exports.chariowWebhook = async (req, res) => {
     } else if (
       normalizedEvent.includes("subscription.cancelled") ||
       normalizedEvent.includes("subscription.canceled") ||
-      normalizedEvent.includes("payment.failed") ||
       normalizedEvent.includes("sale.refunded")
     ) {
+      // Annulation ou remboursement d'un abonnement qui avait été actif :
+      // on révoque l'accès.
       await userRef.set({ subscriptionStatus: "cancelled", updatedAt: new Date() }, { merge: true });
       console.log(`Statut de ${firebaseUid} mis à jour : cancelled`);
+
+    } else if (
+      normalizedEvent.includes("payment.failed") ||
+      normalizedEvent.includes("failed.sale") ||
+      normalizedEvent.includes("sale.failed")
+    ) {
+      // Tentative de paiement échouée (ex : débit Mobile Money non confirmé à
+      // temps côté Chariow). On ne touche pas au statut existant — l'utilisateur
+      // reste "trial"/"expired" comme avant, on journalise juste pour garder une
+      // trace claire de l'échec au lieu de le laisser tomber silencieusement.
+      console.log(`Vente échouée pour ${firebaseUid} (événement : ${eventType}) — aucune réactivation.`);
+      await userRef.set({ lastFailedSaleAt: new Date(), lastFailedSaleEvent: eventType }, { merge: true });
     }
 
     // Répond toujours 200 à Chariow
diff --git a/js/auth-guard.js b/js/auth-guard.js
index 0a0cfd3..b731295 100644
--- a/js/auth-guard.js
+++ b/js/auth-guard.js
@@ -99,6 +99,7 @@ function renderPaywall(user) {
           <label for="paywallCountry" class="paywall__phone-label">Numéro Mobile Money (pour le paiement)</label>
           <div class="paywall__phone-row">
             <select id="paywallCountry" class="paywall__phone-select">${buildCountryOptionsHtml()}</select>
+            <span class="paywall__phone-divider" aria-hidden="true"></span>
             <input type="tel" id="paywallPhone" class="paywall__phone-input" placeholder="8123456789" inputmode="numeric" autocomplete="tel">
           </div>
           <p class="paywall__phone-error" id="paywallPhoneError" hidden>Entrez un numéro Mobile Money valide pour continuer.</p>
@@ -109,6 +110,7 @@ function renderPaywall(user) {
             S'abonner maintenant
           </button>
         </div>
+        <p class="paywall__trust">🔒 Paiement sécurisé via Mobile Money — vous restez connecté à ce compte</p>
 
         <button type="button" id="paywallLogout" class="btn btn-ghost paywall__logout">
           Se déconnecter
diff --git a/js/phone-countries.js b/js/phone-countries.js
new file mode 100644
index 0000000..7ef1367
--- /dev/null
+++ b/js/phone-countries.js
@@ -0,0 +1,34 @@
+/* ==========================================================================
+   Kontra-Africa — Pays & indicatifs pour le paiement Mobile Money
+   Utilisé par auth-guard.js (paywall) et profil.js (réabonnement).
+   ========================================================================== */
+
+// Pays actuellement pris en charge (mêmes marchés que la page d'accueil).
+// `code` = code pays ISO envoyé tel quel au backend (`phone.countryCode`).
+export const COUNTRIES = [
+  { code: 'CD', name: 'RD Congo', dial: '+243', flag: '🇨🇩' },
+  { code: 'CI', name: "Côte d'Ivoire", dial: '+225', flag: '🇨🇮' },
+  { code: 'CM', name: 'Cameroun', dial: '+237', flag: '🇨🇲' },
+  { code: 'SN', name: 'Sénégal', dial: '+221', flag: '🇸🇳' },
+  { code: 'TG', name: 'Togo', dial: '+228', flag: '🇹🇬' },
+  { code: 'BJ', name: 'Bénin', dial: '+229', flag: '🇧🇯' },
+  { code: 'BF', name: 'Burkina Faso', dial: '+226', flag: '🇧🇫' },
+  { code: 'ML', name: 'Mali', dial: '+223', flag: '🇲🇱' },
+  { code: 'GA', name: 'Gabon', dial: '+241', flag: '🇬🇦' },
+  { code: 'CG', name: 'Congo-Brazzaville', dial: '+242', flag: '🇨🇬' },
+];
+
+const DEFAULT_COUNTRY_CODE = 'CD';
+
+/* --- Construit les <option> du <select> pays pour le paiement --- */
+export function buildCountryOptionsHtml(selectedCode = DEFAULT_COUNTRY_CODE) {
+  return COUNTRIES.map((country) => {
+    const selected = country.code === selectedCode ? ' selected' : '';
+    return `<option value="${country.code}"${selected}>${country.flag} ${country.name} (${country.dial})</option>`;
+  }).join('');
+}
+
+/* --- Ne garde que les chiffres d'un numéro saisi (espaces, +, tirets, etc. retirés) --- */
+export function cleanPhoneDigits(value) {
+  return String(value || '').replace(/\D/g, '');
+}
diff --git a/profil.html b/profil.html
index 3272a01..b9b1c54 100644
--- a/profil.html
+++ b/profil.html
@@ -38,6 +38,15 @@
         <h2 class="panel-title">Abonnement</h2>
         <div id="subscription-status" class="subscription-status">
           <p id="subscription-message" class="subscription-message">Chargement…</p>
+
+          <div id="resubscribe-phone-row" class="resubscribe-phone-row" hidden>
+            <label for="resubscribe-phone" class="resubscribe-phone-label">Numéro Mobile Money (pour le paiement)</label>
+            <div class="resubscribe-phone-inputs">
+              <select id="resubscribe-country" class="resubscribe-phone-select"></select>
+              <input type="tel" id="resubscribe-phone" class="resubscribe-phone-input" placeholder="8123456789" inputmode="numeric" autocomplete="tel">
+            </div>
+          </div>
+
           <button type="button" id="btn-resubscribe" class="btn btn-primary" hidden>
             Se réabonner
           </button>
